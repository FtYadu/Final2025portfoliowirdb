/**
 * @fileoverview This file defines the AspectRatio component, a standard UI element.
 * This component is based on the shadcn/ui library and is used to maintain the aspect ratio of a child element.
 * It is styled using Tailwind CSS and built with Radix UI for accessibility and functionality.
 */
import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }
